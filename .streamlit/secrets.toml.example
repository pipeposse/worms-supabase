# Streamlit Community Cloud lee este archivo desde Settings → Secrets.
# NO se commitea (está en .gitignore). Es solo un template.
#
# En Streamlit Cloud: dashboard de tu app → Settings → Secrets → pegar:

DATABASE_URL = "postgresql://postgres.PROYECTO:PASSWORD@aws-0-sa-east-1.pooler.supabase.com:6543/postgres"
